{
  "name": "quizmaster-smart-online-quiz-platform",
  "private": true,
  "version": "1.0.0",
  "description": "Full stack MERN application for creating, taking, and analyzing online quizzes.",
  "scripts": {
    "install:all": "npm.cmd install --prefix server && npm.cmd install --prefix client",
    "dev": "concurrently \"npm.cmd run dev --prefix server\" \"npm.cmd run dev --prefix client\"",
    "server": "npm.cmd run dev --prefix server",
    "client": "npm.cmd run dev --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
